# credentials.yml

# Enable REST API (required for frontend integration)
rest:

# Enable Rasa X or Enterprise API (Optional, for testing via Rasa UI)
rasa:
  url: "http://localhost:5002/api"

# You can also enable other channels like Facebook, Slack, etc., if needed later.
# They’re kept commented for future scalability.
# facebook:
#   verify: "<verify>"
#   secret: "<your secret>"
#   page-access-token: "<your page access token>"

# slack:
#   slack_token: "<your slack token>"
#   slack_channel: "<your channel>"
#   slack_signing_secret: "<your signing secret>"

# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# mattermost:
#   url: "https://<mattermost instance>/api/v4"
#   token: "<bot token>"
#   webhook_url: "<callback URL>"
